[advisories]
# RUSTSEC-2023-0071: Marvin Attack timing side-channel in the `rsa` crate.
# Pulled in transitively by sqlx-mysql (via sqlx's migrate feature) even though
# this project uses PostgreSQL exclusively and performs no RSA operations.
# The crate is unreachable at runtime.
ignore = ["RUSTSEC-2023-0071"]
